<% flash.each do |type, message| %>
  <div id="flash" class="px-4 py-2 bg-<%= type == 'notice' ? 'blue' : 'red' %>-500 text-slate-50 text-sm font-bold rounded w-full absolute z-10">
    <%= simple_format(message) %>
  </div>
<% end %>
